# heiwa.conf: Configuration for Auto-build System
# Heiwa/Linux
# ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
# You have to copy this from `heiwa.conf.default`.

TARGET_ROOTFS="/media/Heiwa"

NATIVE_ARCH="$(uname -m)"

CROSSFLAGS="-flto=thin -march=native -Os -pipe -w -g0"
LDFLAGS="-Wl,-O2 -Wl,--as-needed"

JOBS="$(nproc)"
LOAD="$((${JOBS}+3))"
